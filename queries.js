// Move the common functions from app.js and query.js to this module
const addEmployee = (connection) => {
    // Function implementation
  };
  
  const updateEmployeeRole = (connection) => {
    // Function implementation
  };
  
  // Export the functions
  module.exports = {
    addEmployee,
    updateEmployeeRole,
  };
  